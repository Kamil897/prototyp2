PORT=4000

# URL фронтенда для CORS
FRONTEND_URL=https://твой-сайт.vercel.app

# Строка подключения к PostgreSQL (Railway → Variables → DATABASE_URL)
DATABASE_URL=postgresql://postgres:PASSWORD@host:PORT/railway
